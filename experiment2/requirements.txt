# Experiment server. psiTurk pinned this project to Python 3.9 and ~60
# transitive dependencies (boto3, APScheduler, Flask 1.1, SQLAlchemy 1.3, ...).
# Without it the server needs five packages and runs on current Python.

Flask>=3.0,<4
SQLAlchemy>=2.0,<3
gunicorn>=21.2
user-agents>=2.2
requests>=2.31

# Optional: only needed by scripts/pay_bonuses.py --submit and for Postgres
# deployments. Uncomment if you use them.
# psycopg[binary]>=3.1
